# Indexer

Authority contract: chain events are append-only raw evidence; projections are rebuildable and never override onchain truth. Reorg-aware implementation begins after contract ABIs are frozen.
