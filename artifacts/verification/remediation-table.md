# NexMarkets remediation table

This table is derived from the five failed-verification artifacts and the current implementation. It is a work record; a row is not certified until its verification evidence exists.

| ID | Finding | Severity | Root cause | Files/systems involved | Repair | Verification |
|---|---|---|---|---|---|---|
| 1 | Production can reach shipped project, Edition, listing, owned-Pass, activity, Builder and Pack fixture data | Blocker | The approved HTML initializes `projects`, `projectExperience`, `collections`, `listings`, `ownedPasses`, `dashboardState`, and `createDefaultData` before the API adapter runs. Demo variety functions also publish `demoFinal` and `nmFastDemoAssignment`. | `NEXMARKETS_V2_BUILDER_PROFILE_ELITE.html:4544-4787,13204-13573`; `scripts/build-web.mjs`; `public/nm-v2-data-bridge.js`; `apps/web/public/v2-app.mjs` | Keep the current visual shell, but make the static seed layer an explicit non-production fixture build. Production starts from an empty runtime model and accepts only API/indexer records. | Production-mode authority test scans the built entrypoint and boots routes with an empty backend; fixture authorities must remain unresolved. Existing API/web/browser suites remain green. |
| 2 | Browser storage is used as a Builder-profile authority and partly as runtime state | Blocker | `readStore`/`writeStore` persist Builder profiles in localStorage, and the template owns the social/profile mutation path. | `NEXMARKETS_V2_BUILDER_PROFILE_ELITE.html:14400-14488`; `apps/web/public/v2-app.mjs:533,1093,1283,1800`; `public/nm-v2-data-bridge.js` | Remove domain writes from browser storage. Keep only a documented cosmetic network preference and ephemeral CSRF token; profile, Product, Edition, Pass, listing, Advantage, transaction, earnings and Q&A data come from the backend. | Refresh and two-session tests update/read the backend profile; storage tampering cannot change the returned domain record. |
| 3 | Legacy/current Pass renderers compete for design authority | Blocker | The template exposes `legacyPassHTML`, `nmFinalMountPass` wrappers, `demoFinal`, fast/native Pack and legacy srcdoc branches. | `NEXMARKETS_V2_BUILDER_PROFILE_ELITE.html:4794+,13204-13735`; `packages/domain/src/pass-design.mjs`; `packages/domain/src/launch-draft.mjs`; `apps/web/public/v2-app.mjs:265-457` | Define a frozen canonical configuration and renderer-version compatibility boundary. New Editions use the canonical renderer; historical versions are selected only by explicit `rendererVersion`. Export consumes the same config. | Unit tests enumerate new design options and assert no obsolete authority is selected; historical compatibility is separately exercised. |
| 4 | Primary proceeds are hard-coded to zero | Blocker | `emptyDashboard`, `buildDashboardData`, and Builder profile stats set `primaryProceeds`/`totalVolumeUsdg` to zero; there is no primary-sale accounting projection. | `apps/web/public/v2-app.mjs:398,1534`; `apps/api/src/memory-store.mjs:62`; `packages/data/src/postgres-store.mjs:401,485`; `infra/schema/0002_nexmarkets_v1.sql` | Add an idempotent primary-sale accounting projection derived from confirmed mint/payment evidence. Store gross, 5% fee, Builder proceeds, referral obligation, tx hash, token and network; aggregate it in dashboard/profile authority. | Gross 10 → fee .50 → Builder 9.50; multi-sale aggregation; failed/duplicate event idempotency; referral separation; Postgres and memory tests. |
| 5 | One user cannot control multiple Builder identities | Blocker | `builder_profile.account_id` is `UNIQUE`; project/social/dashboard queries are keyed only by account. | `infra/schema/0003_social_and_builder_profiles.sql:3-14`; `apps/api/src/memory-store.mjs:9,131-178`; `packages/data/src/postgres-store.mjs:485-530`; `apps/api/src/server.mjs:576-709` | Add persistent Builder identities and memberships, preserve existing IDs, put project/profile mutations behind an authorized `builderId`, and add a safe UI selector. | Same user controls A/B; edits and Products stay isolated; another user receives rejection; server ignores/treats client selection as untrusted. |
| 6 | PostgreSQL migration/no-data-loss was not verified | Blocker | No usable `DATABASE_URL`/PostgreSQL runtime was available, and no executable before/after fixture report existed. | `infra/schema/migrate.mjs`; `infra/schema/0001-0007*.sql`; `packages/data/src/postgres-store.mjs`; `artifacts/verification/final-report.md` | Add the multi-Builder/accounting migration, a realistic snapshot-and-replay report script, orphan/duplicate checks, and CI integration. Record blocked status rather than inventing counts when infrastructure is absent. | Actual pre/post counts, representative IDs/values, FK/orphan/duplicate checks, migration test suite. |
| 7 | Six Node tests are skipped | High | All six are conditional Postgres/indexer/reorg tests and skip when `DATABASE_URL` is absent. | `test/runtime-integration.test.mjs:30,65`; `test/reconciliation.test.mjs:109,118`; `test/chain-worker-integration.test.mjs:6`; `test/indexer-postgres-reorg.integration.test.mjs:28` | Document each as environment-specific, add a migration test and an explicit CI command that runs the full integration set when PostgreSQL is provisioned. | Skip inventory is generated; production-critical tests execute in the DB job, or the report remains blocked with the dependency named. |
| 8 | No fresh live testnet transaction certification | Blocker | Browser acceptance uses fake EIP-1193/intercepted APIs; historical JSON evidence is not a fresh certification run. | `test/browser/webapp.acceptance.spec.mjs`; `scripts/certify-*.mjs`; `artifacts/testnet-certification/*`; configured Robinhood testnet RPC | Run the real intended testnet lifecycle if funded wallets and RPC are available; persist actual hashes/blocks/owners. Never substitute fixtures or historical hashes. | `artifacts/verification/testnet-certification.md` with primary, Advantage, listing, ask change, cancel/relist, secondary purchase, transfer and royalty evidence—or an explicit infrastructure blocker. |
| 9 | Payment token is MockUSDG without explicit scope/display | Blocker | Robinhood testnet config points to the MockUSDG deployment but labels it simply `USDG`; production token configuration is not represented as a chain-scoped readiness gate. | `scripts/render-web-testnet-config.mjs:44-45`; `scripts/render-web-config.mjs:53-54`; `deployments/robinhood-testnet.mock-usdg.json`; `public/config.json`; contract deployment manifests | Centralize settlement-token config by chain, label Robinhood as testnet-only MockUSDG, and require a separately verified production token before production readiness can be true. | Config verifier checks address/network/scope; frontend displays actual settlement token; production gate fails closed until real production token is configured. |
| 10 | `productionReady:false` is a loose/manual field rather than derived certification | Blocker | Render scripts write a literal boolean and do not evaluate migration, token, testnet, security or critical-test evidence. | `scripts/render-web-testnet-config.mjs:64`; `scripts/render-web-config.mjs:88`; `public/config.json` | Add explicit readiness gates and derive the boolean from them. Keep false while any required gate is absent. | Gate evaluator tests each condition and verifies no manual true value is accepted. |
| 11 | Performance/accessibility/security evidence is incomplete | High | Existing visual JSON measures overflow/iframes but no full performance, automated a11y, keyboard or negative-security report was produced. | `artifacts/verification/final-visual/visual.json`; `test/browser/*`; API auth/order/intents | Add runnable performance, accessibility and negative-security audits; fix serious findings while preserving current green tests. | `artifacts/verification/performance-report.md`, `accessibility-report.md`, `security-report.md` with measured results. |
| 12 | Full human journey and independent recertification are absent | Blocker | Existing browser evidence relies on harness mutations and does not prove a real Builder→purchase→secondary→social journey. | `test/browser/webapp.acceptance.spec.mjs`; `artifacts/verification/browser-final2/*`; `artifacts/verification/final-report.md` | Run the original journey against the repaired authorities, preserving screenshots/API/chain evidence; then invoke the original certification command/prompt. | Exact final verifier output must be `PRODUCTION MIGRATION VERIFIED`; otherwise this row stays open. |

## Current resolution status

| IDs | Status | Evidence |
|---|---|---|
| 1–5 | PASS | Authority, renderer, primary-accounting and multi-Builder tests pass. |
| 6 | PASS | Migrations 0001–0009 were applied to the configured PostgreSQL 17.6 target; isolated before/after preservation and target-registry checks pass. |
| 7 | PASS | The six formerly skipped PostgreSQL/indexer/reorg tests executed; Node result is 131 passed, 0 skipped, 0 failed. |
| 8 | PARTIAL | Ten receipts, RPC state, Goldsky and local API corroboration pass; change-ask/replacement-order evidence is not proven. |
| 9–11 | PASS | Token scoping/readiness derivation and performance/accessibility/security evidence pass. |
| 12 | BLOCKED | A fresh complete funded-wallet journey and independent recertification remain outstanding. |
