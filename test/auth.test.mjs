import test from 'node:test'; import assert from 'node:assert/strict';
import { issueWalletChallenge, assertChallengeUsable, consumeChallenge } from '../packages/auth/src/index.mjs';
test('wallet challenge is single use and domain bound',()=>{const now=1000;const c=issueWalletChallenge({accountId:'acct_x',address:'0x1111111111111111111111111111111111111111',origin:'https://nexmarkets.fun',ttlSeconds:60,now});assert.equal(assertChallengeUsable(c,{accountId:'acct_x',address:c.address,origin:c.origin,now:2000}),true);const used=consumeChallenge(c,2000);assert.throws(()=>assertChallengeUsable(used,{accountId:'acct_x',address:c.address,origin:c.origin,now:3000}));assert.throws(()=>assertChallengeUsable(c,{accountId:'acct_x',address:c.address,origin:'https://evil.example',now:2000}));});
