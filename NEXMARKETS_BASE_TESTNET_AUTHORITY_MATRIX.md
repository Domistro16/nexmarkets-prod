# NexMarkets — Base Testnet Authority Matrix

Generated by direct inspection of the repository, live Base Sepolia reads, a fresh
Foundry run, and a forked-chain certification journey.

**Frontend authority (user-designated):**
`NEXMARKETS_HOMEPAGE_DISCOVER_MARKET_COLLECTIBLE_ROTATION_PASS_TEXT_FIT_UX_FIXED.html`
`sha256 7e67e10e8332677668baab0e5653e84c62556d57f816f5b09daa6337adf9858c`

> The file named in the original brief, `NEXMARKETS_HOMEPAGE_HERO_LOAD_OPTIMIZED_FINAL.html`,
> **does not exist** anywhere in the workspace. The file above is the latest named snapshot.

**Served build:** `dist/index.html` == `public/index.html`
`sha256 2acd62eda4ba49fa502fd01b29f34656fc42ecebed6b31cd9e26160705b6f748`

The served build is a strict superset of the approved snapshot. The only differences are
additive integration wiring, confirmed by full diff:

| Added in served build | Purpose |
|---|---|
| `<meta name="base:app_id">` | Base miniapp registration |
| `__NEXMARKETS_FIXTURE_MODE__ = false` | Fixture-mode kill switch, **off** in the served build |
| `rainbowkit-bridge.css`, `cdp-auth-bridge.css` | Wallet/auth UI |
| `nm-v2-data-bridge.js` | Overwrites the demo app's global arrays with API data |
| `v2-app.mjs` (232 KB) | Real API + chain integration layer |

**No UI regression.** Homepage, Discover, Market, Dashboard, Create, Builder profiles,
Project/Collection, Exact Pass, Pass rendering/colour system, mobile four-Pass hero,
charts, Pass Vault, Advantages & Rewards and Launch & Access are all byte-identical
between the approved snapshot and the served build.

---

## Classification key

| Label | Meaning |
|---|---|
| **VERIFIED** | Real authority exists and was successfully exercised in this session |
| **IMPLEMENTED BUT UNVERIFIED** | Code exists; real execution was not proven here |
| **PARTIAL** | Some authority exists but UI behaviour is not fully backed |
| **MOCKED** | UI/local simulation only |
| **MISSING** | Required implementation does not exist |

---

## 1. Create / publish

| Capability | UI | Backend | Contract | Indexer | Status |
|---|---|---|---|---|---|
| Edition deployment | Yes | `/v1/builder/projects` | `NexPassFactory.createEdition` | `EditionCreated` | **VERIFIED** (fork) |
| Permissionless Builder publish | Yes | `/v1/terms/prepare` | `NexLaunchRegistry.publishTerms` | `TermsPublished` | **VERIFIED** (fork) |
| Immutable terms hash | Yes | `/v1/terms/hash` | `hashTerms` (domain-separated) | `TermsVersion` | **VERIFIED** (fork) |
| Supply / price / royalty | Yes | Yes | `Terms` struct | Yes | **VERIFIED** (fork) |
| Builder Royalty cap 5% | Yes | Yes | `MAX_ROYALTY_BPS = 500` | Yes | **VERIFIED** |
| Artwork commitment | Yes | Yes | `artworkCommitment` | Yes | **IMPLEMENTED BUT UNVERIFIED** |
| Media upload | Yes | `/v1/media/uploads` (R2) | n/a | n/a | **IMPLEMENTED BUT UNVERIFIED** |

## 2. Terms version / Preview authority

| Capability | Status | Evidence |
|---|---|---|
| Material change creates new terms version | **VERIFIED** | `publishTerms` increments `nextTermsVersion`, rehashes |
| Preview restarts on republish | **VERIFIED** | `previewStartsAt >= block.timestamp` + `MIN_PREVIEW_DURATION = 1 days` |
| Minted Passes keep their own terms | **VERIFIED** | `termsVersionHashOf(tokenId)` is snapshotted at mint |
| Live terms immutable **after** first mint | **PARTIAL** | Publisher may still publish a new version post-mint; guarded only by `activeSupply >= totalMinted` and a forced 1-day Preview |
| API cannot bypass | **IMPLEMENTED BUT UNVERIFIED** | Registry enforces `onlyEditionPublisher`; backend has no privileged path |

## 3. Launch & Access / Early Access

| Capability | Status | Evidence |
|---|---|---|
| Early Access OFF by default | **VERIFIED** | `allowlistRoot == 0` forces all allowlist fields to 0 |
| Early Access window | **VERIFIED** | `allowlistEndsAt`, fork journey step 3 |
| Capped allocation (500 of 2000) | **VERIFIED** | `allowlistSupply`, `AllowlistSupplyExceeded` |
| **Per-wallet allowance (max 2/wallet)** | **VERIFIED — FIXED THIS SESSION** | Was **MISSING**. Now `Terms.walletAllowance` + `allowlistWalletMinted`; mint #3 reverts `WalletAllowanceExceeded` |
| Public Mint opens automatically | **VERIFIED** | `isPublicMintOpen` is purely `block.timestamp >= allowlistEndsAt`; no Builder tx |
| Unused allocation rolls over | **VERIFIED** | `allowlistSupply` is a sub-cap of `activeSupply`; fork journey step 4 |
| Ineligible wallet blocked at contract | **VERIFIED** | `NotAllowlisted`; Charlie's direct call reverts |

## 4. Allowlist authority

| Capability | Status | Evidence |
|---|---|---|
| Merkle root committed to terms | **VERIFIED** | `Terms.allowlistRoot`, `MintAccessPublished` |
| Server-side proof generation | **IMPLEMENTED BUT UNVERIFIED** | `/v1/allowlists/merkle` exists; not executed |
| **Leaf binds Edition + chain + allowance** | **VERIFIED — FIXED THIS SESSION** | Was `keccak256(keccak256(abi.encode(account)))` (wallet only). Now binds `chainid, controller, edition, account, allowance` |
| Cross-Edition replay blocked | **VERIFIED — FIXED THIS SESSION** | `testAllowlistProofCannotBeReplayedOntoAnotherEdition` |
| Wallet cannot inflate own allowance | **VERIFIED — FIXED THIS SESSION** | `testWalletCannotInflateItsOwnAllowance` |
| Allowlist not publicly enumerable | **IMPLEMENTED BUT UNVERIFIED** | Only root is on chain |

## 5. Mint & settlement

| Capability | Status | Evidence |
|---|---|---|
| USDC settlement (canonical Base Sepolia) | **VERIFIED** | `0x036CbD53…f3dCF7e`, 6dp, real token on fork |
| 5% primary protocol fee | **VERIFIED** | `PROTOCOL_FEE_BPS = 500`; exact balances asserted in fork journey |
| Builder primary proceeds | **VERIFIED** | fork journey step 3 |
| Payer-scoped intent idempotency | **VERIFIED** | `_consumedIntent[payer][intentId]` |
| Serial assignment | **VERIFIED** | Serials 1, 2, 3 asserted |
| Referral | **PARTIAL** | `ReferralHintSubmitted` is explicitly non-canonical; backend ledger not verified |

## 6. Holder Advantages

| Capability | Status | Evidence |
|---|---|---|
| Edition policy committed | **VERIFIED** | `advantagesHash` in Terms |
| Per-Pass state | **VERIFIED** | `NexAdvantageRegistry` per `(edition, tokenId)` |
| Consumption authoritative | **VERIFIED** | 10 credits → consume 3 → 7 remain (fork step 5) |
| Idempotent use IDs | **VERIFIED** | Replay of same `useId` does not double-spend |
| Remaining state follows Pass on transfer | **VERIFIED** | 7 credits retained by buyer (fork step 9) |
| Frozen while listed | **VERIFIED** | `setListed` → `isUsable == false` |
| Frontend cannot reset | **VERIFIED** | Registry is the only writer; `onlyListingAuthority` / `onlyInitializer` |

## 7. Pass Vault / ERC-6551

| Capability | Status | Evidence |
|---|---|---|
| Deterministic TBA per Pass | **VERIFIED** | Canonical registry `0x000000006551c19487814612e58FE06813775758` on fork |
| Owner control resolves from `ownerOf` | **VERIFIED** | fork step 9 |
| Vault follows Pass on transfer | **VERIFIED** | Same address, same 4 remaining assets after sale |
| Seller loses control after transfer | **VERIFIED** | `InvalidSigner` on old owner |
| Multi-asset (>2) support | **VERIFIED** | 5 distinct assets: USDC, NVDAc, NEX, OP, ERC-721 |
| **Vault locked while listed** | **VERIFIED — FIXED THIS SESSION** | Was **MISSING** — vanilla ERC-6551 account allowed the seller to drain a listed Pass. Now `PassVaultLockedWhileListed` |
| Deposits allowed while listed | **VERIFIED** | `receive()` and inbound transfers unaffected |
| Expired listing does not brick Vault | **VERIFIED** | `testExpiredListingDoesNotPermanentlyLockTheVault` |
| **Multi-asset claim UI → backend/tx** | **IMPLEMENTED + TESTED** | `POST /v1/vault/claims/prepare` resolves the TBA, verifies Pass owner, `isVaultLocked()`, asset balance/owner and builds one confirmed `execute` per asset; browser uses BigInt base units and ERC-721 whole-only controls |

## 8. Rewards

| Capability | Status | Evidence |
|---|---|---|
| Reward Policy model | **DEPLOYED + INDEXED** | `NexRewardDistributor.publishPolicy` at `0x2453c5FC…EB91`; subgraph `RewardPolicy`; `GET /v1/editions/:address/rewards` |
| Reward Cycle model | **DEPLOYED + INDEXED** | `fundCycle` escrows `amountPerPass × eligibleSupply`; live subgraph `RewardCycle`; status is `FUNDED` by construction |
| Claim into Pass Vault | **VERIFIED** (unit) | Permissionless `claim` / `claimMany`; credits the TBA, including while listed; 23/23 tests |
| Tokenized stocks / tokens / airdrop (ERC-20) | **DEPLOYED** | Equal-split ERC-20 path. Fee-on-transfer rejected |
| Collectible drops (ERC-721) | **OUT OF SCOPE** | Unique NFTs cannot be split equally; not modelled |
| Allocation % enforcement | **PUBLISHED COMMITMENT ONLY** | `allocationBps` is indexed, not collected from `NexRoyaltyVault` |
| Distinction policy vs guaranteed amount | **VERIFIED** (unit) | `testPublishPolicyCommitsNoFunds` / `testFundCycleEscrowsExactEqualSplit` |

## 9. Market / Seaport

| Capability | Status | Evidence |
|---|---|---|
| Seaport 1.6 canonical address | **VERIFIED** | `0x00000000…eB395` has code on Base Sepolia |
| Listing creation + registry state | **VERIFIED** | fork step 8 |
| Zone restriction | **VERIFIED** | Both callbacks fire on a real fill; zone reverts `ConsiderationMismatch` on a signed, registered order underpaying by 1 base unit |
| Listing cancel releases locks | **VERIFIED** | fork step 9 |
| Settlement releases locks | **VERIFIED** | Vault and Advantage locks clear on fill, no extra transaction |
| Expiry / stale-order handling | **VERIFIED** (unit) | `isListingActive` checks status, window, `ownerOf == seller` |
| **Real Seaport order fulfilment (buy)** | **VERIFIED** | `SeaportFulfillmentFork.t.sol` — signed EIP-712 restricted order fulfilled through canonical Seaport 1.6 |
| Secondary fee / royalty routing | **VERIFIED** | 250 USDC sale splits exactly 2.5 / 12.5 / 235 with the buyer debited once; `recordRoyalty` lands inside the atomic fill |
| Royalty 30-day hold | **VERIFIED** | Rejects early withdrawal and non-Builder callers |
| Conduit-based fulfilment | **IMPLEMENTED BUT UNVERIFIED** | Tests use a zero conduit key with direct approvals |
| Partial / advanced orders, `matchOrders`, buyer offers | **IMPLEMENTED BUT UNVERIFIED** | Not exercised |

## 10. Dashboard / Discover / indexing

| Capability | Status | Evidence |
|---|---|---|
| Subgraph deployed | **VERIFIED** | Rebuilt and redeployed to Goldsky against the 11-contract address set from block 46,827,960; Active/100% synced with no indexing errors |
| 17 indexed entities | **VERIFIED** (schema) | `subgraph/schema.graphql`, including `TermsVersion.walletAllowance`, `RewardPolicy`, `RewardCycle` and `RewardClaim` |
| Deterministic event identity | **IMPLEMENTED BUT UNVERIFIED** | `chainId:txHash:logIndex` in `projector.mjs` / `runtime.mjs` — correct design |
| Reorg / orphan handling | **IMPLEMENTED BUT UNVERIFIED** | `orphaned_at`, watermarks, finality block in `runtime.mjs` |
| Dashboard surfaces | **PARTIAL** | Passes/Advantages/Listings/Activity have real endpoints; Vault assets still do not. Reward *reads* exist; the dashboard does not consume them yet |
| Collection & exact-Pass charts | **IMPLEMENTED BUT UNVERIFIED** | Backed by indexed entities; `rewardDeposited` / `vaultClaim` event types absent |

## 11. Backend / API

| Capability | Status | Evidence |
|---|---|---|
| 39 `/v1` endpoints | **IMPLEMENTED BUT UNVERIFIED** | `apps/api/src/server.mjs`, including authenticated Vault claim preparation |
| 28 called by the frontend | **VERIFIED** (static) | Enumerated in `v2-app.mjs` — real integration, not stubs |
| Postgres store | **IMPLEMENTED BUT UNVERIFIED** | `PostgresStore`; `MemoryStore` fallback only when `DATABASE_URL` unset |
| Production guard on DB | **VERIFIED** (static) | `DATABASE_URL_REQUIRED_FOR_PRODUCTION` |
| SIWE-style session + CSRF | **IMPLEMENTED BUT UNVERIFIED** | 97 session refs, 13 CSRF refs |
| Rate limiting | **VERIFIED** (static + test) | **Earlier "MISSING" was wrong.** `RateLimiter` in `server.mjs` throws 429 `RATE_LIMITED`, applied per request keyed on remote address, 300 req / 60,000 ms; covered by the passing test "same-origin and rate-limit controls fail closed" |

## 12. Removed false attestation

The former hardcoded `nmVaultClaimAudit()` capability object and the
`completeSelectedMarketPurchase`/`setTimeout` local Vault transfer simulation have
been deleted. Neither is evidence. Current Vault claim evidence comes from API builder
tests, contract tests, and confirmed wallet receipts; public Base Sepolia claim
transactions are still outstanding.
